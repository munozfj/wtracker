<% page_title "Photo Gallery" %>

<div class="jumbotron">
<h1>Photo Gallery</h1>

<%= link_to "<button type='button' class='btn btn-default active'>
  <span class='glyphicon glyphicon-plus-sign'></span> </button>".html_safe , new_photo_path %>
  
  <br />
  <br />

 
<!-- table class="table table-striped table-bordered table-hover table-condensed">
  <thead>
    <tr>
      <th>Date</th>
      <th>Picture</th>
      <th>Comment</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    < @photos.each do |photo| >
      <tr>
        <td><= photo.date ></td>
        <td> < if photo.picture.exists? >
            <= image_tag photo.picture.url(:thumb)>
          < end >

        </td>
        <td><= photo.comment ></td>
        <td><= link_to 'Show', photo ></td>
        <td><= link_to 'Edit', edit_photo_path(photo) ></td>
        <td><= link_to 'Destroy', photo, method: :delete, data: { confirm: 'Are you sure you want to delete this picture?' } ></td>
      </tr>
    < end >
  </tbody>
</table -->


<div id="carousel-example-generic" class="carousel slide" data-ride="carousel">
  <!-- Definición de Tabs -->
  <ol class="carousel-indicators">
    <!--li data-target="#carousel-example-generic" data-slide-to="0" class="active"></li>
    <li data-target="#carousel-example-generic" data-slide-to="1"></li>
    <li data-target="#carousel-example-generic" data-slide-to="2"></li-->

      <% it=0 %>
      <% @photos.each do |photo| %>
        <% if it == 0 %>
          <li data-target="#carousel-example-generic" data-slide-to="<%= it %>" class="active"></li>
        <% else %>
          <li data-target="#carousel-example-generic" data-slide-to="<%= it %>" ></li>
        <% end %>

        <% it+=1 %>

      <% end %>
  </ol>

  <!-- Wrapper for slides -->
  <div class="carousel-inner">
    <!-- div class="item active">
      <img src="..." alt="...">
      <div class="carousel-caption">
        Hola
      </div>
    </div -->

    <% it=0 %>
      <% @photos.each do |photo| %>
        <% if it == 0 %>
          <div class="item active">
        <% else %>
          <div class="item ">
        <% end %>

      <% if photo.picture.exists? %>
            <%= image_tag photo.picture.url(:medium)%>
          <% end %>
    </div>

        <% it+=1 %>

      <% end %>

  </div>

  <!-- Flechas de Desplazamiento -->
  <a class="left carousel-control" href="#carousel-example-generic" data-slide="prev">
    <span class="glyphicon glyphicon-chevron-left"></span>
  </a>
  <a class="right carousel-control" href="#carousel-example-generic" data-slide="next">
    <span class="glyphicon glyphicon-chevron-right"></span>
  </a>
</div>


<br>

<%= link_to "<button type='button' class='btn btn-default active'>
  <span class='glyphicon glyphicon-chevron-left'></span> Panel </button>".html_safe , panel_path %>

</div>
